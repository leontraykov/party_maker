<p style="color: green"><%= notice %></p>

<h1><%= t 'views.users.users' %></h1>

<div id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <p>
      <%= link_to I18n.t('views.users.show'), user %>
    </p>
  <% end %>
</div>

<%= link_to I18n.t('views.users.new_user'), new_user_path %>
